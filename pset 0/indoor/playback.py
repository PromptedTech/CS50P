# Ask the user to type something
yo = input('hello: ')

# Replace every space with "..." to slow down the playback effect
# For example "hello world" becomes "hello...world"
print(yo.replace(" ", "..."))
