# Ask the name of the user, strip out the whitespaces in the str, and capatilize the first letter of the name
name = input('What is your name? ').title().strip()

# Say hello to user
print(f'hello, {name}')
